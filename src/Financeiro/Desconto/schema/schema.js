import Joi from "joi";

const Schema = Joi.object({
/*     NOMEDODADO: Joi.number().required()
        .messages({
            "any.required": "O ID do resumo da OT é obrigatório",
            "number.base": "O ID do resumo da OT deve ser um número"
        }), */
    })
export default Schema;